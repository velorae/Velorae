# Velorae Landing Page

Open `index.html` in a browser to preview the landing page.

The email form uses Buttondown's embedded subscribe form. It works without a custom backend, but Buttondown may show its own confirmation page after submit.

Recommended bio CTA:

```text
Building Velorae - a calm journal app for mood tracking and gentle AI insights.
Join early access ↓
```

Recommended video CTA:

```text
Would you use Velorae?
Join early access - link in bio.
```
